{
  "name": "lambda-function-url-handler",
  "version": "1.0.0",
  "description": "AWS Lambda Function URL handler with DynamoDB",
  "type": "module",
  "main": "dist/handler.js",
  "scripts": {
    "build": "esbuild handler.ts --bundle --platform=node --target=node20 --format=esm --outfile=dist/handler.js --external:@aws-sdk/*",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["aws", "lambda", "dynamodb", "typescript"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.138",
    "@types/node": "^20.11.5",
    "esbuild": "^0.20.0",
    "typescript": "^5.3.3"
  }
}
